# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'easyupload' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for easyupload
  pod 'Alamofire', '~> 4.4’

  # resume upload
  pod 'TUSKit', :path => 'easyupload/Libraries/TUSKit/'

end
